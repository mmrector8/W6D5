<h1> Cat Rental Request Form </h1>

<form action= "<%=cat_rental_requests_url%>" method = "POST">

<label> Cat Desired
    <select name = "cat_rental_request[cat_id]">
    <option disabled selected> Please choose a cat! </option>
    <% Cat.all.each do |cat| %>
        <% selected = @rental_request.cat_id == cat.id %>
        <option <%= selected ? "selected" : "" %>
                value="<%= cat.id %>">
          <%= cat.name %>
        </option>
    <% end %>
    required
    </select>
</label> <br> <br>

<label>  Requested Start Date:
    <input type="date" name="cat_rental_request[start_date]" value="<%= @rental_request.start_date %>"required>
</label> <br><br>

<label>  Requested End Date:
    <input type = "date" name = "cat_rental_request[end_date]" value = "<%=@rental_request.end_date%>" required>
</label> <br><br>

<input type = "submit" value = "Submit Request">


</form> 